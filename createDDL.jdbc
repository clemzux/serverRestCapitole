CREATE TABLE CDATE (date_id INTEGER NOT NULL, DATE VARCHAR(255), PRIMARY KEY (date_id))
CREATE TABLE CRESERVATION (reservation_id INTEGER NOT NULL, NAME VARCHAR(255), NBMEAL INTEGER, NBPEOPLE INTEGER, NOTE VARCHAR(255), date INTEGER, PRIMARY KEY (reservation_id))
ALTER TABLE CRESERVATION ADD CONSTRAINT FK_CRESERVATION_date FOREIGN KEY (date) REFERENCES CDATE (date_id)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('dateGenerator', 0)
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('reservationGenerator', 0)
